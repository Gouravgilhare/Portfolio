version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: DockerFile
    container_name: portfolio-app
    env_file:
      - .env
    ports:
      - "5000:5000"
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
